<!DOCTYPE html>
<html>
<head>
    <title>User Report</title>
</head>
<body>
    <h1>Welcome, {{name}}!</h1>
    <p>Email: {{email}}</p>
    <p>Age: {{age}}</p>

    <h2>Orders</h2>
    <ul>
        {{#each orders}}
            <li>Order ID: {{this.id}}, Amount: ${{this.amount}}</li>
        {{/each}}
    </ul>
</body>
</html>
